/**
 * Generated by orval v7.13.2 🍺
 * Do not edit manually.
 * ShieldNet Gateway Control Plane API
 * REST API for ShieldNet Gateway (SNG) — multi-tenant control plane.
All operator endpoints are mounted under `/api/v1`. Auth is
either a bearer JWT (operator console) or `X-SNG-API-Key`
(machine-to-machine).

 * OpenAPI spec version: 0.1.0
 */
import type { TenantMigrationState } from './tenantMigrationState';
import type { TenantMigrationCheckpoint } from './tenantMigrationCheckpoint';

export interface TenantMigration {
  id: string;
  tenant_id: string;
  source_region: string;
  target_region: string;
  state: TenantMigrationState;
  /** True while the tenant is served from both source and target regions during an in-flight migration, so no traffic is lost.
 */
  dual_read: boolean;
  /** Human-readable detail for the current state (e.g. failure cause). */
  detail?: string;
  attempts: number;
  /** Opaque per-step checkpoint the state machine resumes from; defaults to an empty object before the first step records.
 */
  checkpoint: TenantMigrationCheckpoint;
  created_at: string;
  updated_at: string;
  started_at?: string;
  completed_at?: string;
}
