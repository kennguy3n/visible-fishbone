/**
 * Generated by orval v7.13.2 🍺
 * Do not edit manually.
 * ShieldNet Gateway Control Plane API
 * REST API for ShieldNet Gateway (SNG) — multi-tenant control plane.
All operator endpoints are mounted under `/api/v1`. Auth is
either a bearer JWT (operator console) or `X-SNG-API-Key`
(machine-to-machine).

 * OpenAPI spec version: 0.1.0
 */

export type CreateAlertSuppressionBody = {
  /** Alert kind to suppress, e.g. baseline_zscore_anomaly. Either `kind` or `dimension` must be set. */
  kind?: string;
  /** Baseline dimension to suppress (e.g. flow_bytes_inbound). Either `kind` or `dimension` must be set. */
  dimension?: string;
  /** Operator-visible audit note documenting why this rule exists. */
  reason: string;
  /** Optional auto-expiry; the rule stops applying once expires_at has passed (the row remains for audit). */
  expires_at?: string;
};
