/**
 * Generated by orval v7.13.2 🍺
 * Do not edit manually.
 * ShieldNet Gateway Control Plane API
 * REST API for ShieldNet Gateway (SNG) — multi-tenant control plane.
All operator endpoints are mounted under `/api/v1`. Auth is
either a bearer JWT (operator console) or `X-SNG-API-Key`
(machine-to-machine).

 * OpenAPI spec version: 0.1.0
 */

export interface MobileTokenRequest {
  /** OIDC ID token obtained by the mobile agent's native sign-in. */
  id_token: string;
  /** Optional issuer selector. When omitted the issuer is read
from the (unverified) token's `iss` claim and matched
against the tenant's enabled configs.
 */
  issuer?: string;
  /** Base64 Ed25519 device public key the session is bound to. */
  device_public_key: string;
}
