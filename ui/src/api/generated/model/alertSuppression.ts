/**
 * Generated by orval v7.13.2 🍺
 * Do not edit manually.
 * ShieldNet Gateway Control Plane API
 * REST API for ShieldNet Gateway (SNG) — multi-tenant control plane.
All operator endpoints are mounted under `/api/v1`. Auth is
either a bearer JWT (operator console) or `X-SNG-API-Key`
(machine-to-machine).

 * OpenAPI spec version: 0.1.0
 */

export interface AlertSuppression {
  id: string;
  tenant_id: string;
  /** Alert kind matched by this rule. Either kind or dimension (or both) is set; nil means match any kind within the supplied dimension. */
  kind?: string;
  /** Baseline dimension matched by this rule. Either kind or dimension (or both) is set; nil means match any dimension within the supplied kind. */
  dimension?: string;
  reason: string;
  /** Operator who created the rule. Derived from the JWT sub / API-key owner server-side. */
  created_by?: string;
  created_at: string;
  expires_at?: string;
}
