/**
 * Generated by orval v7.13.2 🍺
 * Do not edit manually.
 * ShieldNet Gateway Control Plane API
 * REST API for ShieldNet Gateway (SNG) — multi-tenant control plane.
All operator endpoints are mounted under `/api/v1`. Auth is
either a bearer JWT (operator console) or `X-SNG-API-Key`
(machine-to-machine).

 * OpenAPI spec version: 0.1.0
 */
import type { ManagedThreatContentSourceStatusKind } from './managedThreatContentSourceStatusKind';

/**
 * Per-feed ingestion health row.
 */
export interface ManagedThreatContentSourceStatus {
  name: string;
  display_name: string;
  /** Indicator kind the feed contributes. */
  kind: ManagedThreatContentSourceStatusKind;
  /** Source trust weight used in corroboration scoring. */
  weight: number;
  enabled: boolean;
  indicator_count: number;
  last_success_at?: string;
  last_attempt_at?: string;
  last_error?: string;
  consecutive_failures: number;
  /** True when the feed has never succeeded, is currently failing, or its last success is older than the staleness window. */
  stale: boolean;
}
