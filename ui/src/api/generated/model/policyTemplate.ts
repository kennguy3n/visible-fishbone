/**
 * Generated by orval v7.13.2 🍺
 * Do not edit manually.
 * ShieldNet Gateway Control Plane API
 * REST API for ShieldNet Gateway (SNG) — multi-tenant control plane.
All operator endpoints are mounted under `/api/v1`. Auth is
either a bearer JWT (operator console) or `X-SNG-API-Key`
(machine-to-machine).

 * OpenAPI spec version: 0.1.0
 */
import type { PolicyTemplateKind } from './policyTemplateKind';
import type { PolicyTemplateSpec } from './policyTemplateSpec';

/**
 * A catalog template. `kind` is one of `baseline`, `industry`,
or `compliance`; `industry` is set only for industry templates
and `regime` only for compliance templates. `spec` carries the
URL categories, DLP detectors, and firewall posture the
renderer composes into a Policy-Graph.

 */
export interface PolicyTemplate {
  id?: string;
  kind?: PolicyTemplateKind;
  name?: string;
  description?: string;
  industry?: string;
  regime?: string;
  spec?: PolicyTemplateSpec;
}
