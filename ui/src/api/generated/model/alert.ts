/**
 * Generated by orval v7.13.2 🍺
 * Do not edit manually.
 * ShieldNet Gateway Control Plane API
 * REST API for ShieldNet Gateway (SNG) — multi-tenant control plane.
All operator endpoints are mounted under `/api/v1`. Auth is
either a bearer JWT (operator console) or `X-SNG-API-Key`
(machine-to-machine).

 * OpenAPI spec version: 0.1.0
 */
import type { AlertSeverity } from './alertSeverity';
import type { AlertEvidence } from './alertEvidence';
import type { AlertState } from './alertState';

export interface Alert {
  id: string;
  tenant_id: string;
  /** Detector kind, e.g. baseline_zscore_anomaly. */
  kind: string;
  severity: AlertSeverity;
  /** Baseline dimension the alert was generated against. */
  dimension: string;
  /** Observation that tripped the detector. */
  observed_value: number;
  baseline_mean: number;
  baseline_stddev: number;
  /** max(|z_welford|, |z_ewma|) — the deviation score that tripped the detector */
  z_score: number;
  window_start: string;
  window_end: string;
  /**
   * Baseline window size (in seconds) the alert was generated against. Identifies which (dimension, window_seconds) baseline bucket emitted this alert — the same dimension can have multiple buckets (e.g. 60 vs 3600).
   * @minimum 1
   */
  window_seconds: number;
  /** Human-readable rationale string the detector emitted with the alert. */
  summary: string;
  /** Detector-supplied JSON payload (statistical context, thresholds, etc.). */
  evidence?: AlertEvidence;
  state: AlertState;
  /** Suppression rule ID that auto-routed this alert to the suppressed state, if any. */
  suppressed_by?: string;
  acknowledged_by?: string;
  acknowledged_at?: string;
  resolved_by?: string;
  resolved_at?: string;
  created_at: string;
  updated_at: string;
}
