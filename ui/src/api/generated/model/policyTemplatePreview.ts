/**
 * Generated by orval v7.13.2 🍺
 * Do not edit manually.
 * ShieldNet Gateway Control Plane API
 * REST API for ShieldNet Gateway (SNG) — multi-tenant control plane.
All operator endpoints are mounted under `/api/v1`. Auth is
either a bearer JWT (operator console) or `X-SNG-API-Key`
(machine-to-machine).

 * OpenAPI spec version: 0.1.0
 */
import type { PolicyTemplateSelection } from './policyTemplateSelection';
import type { PolicyTemplatePreviewGraph } from './policyTemplatePreviewGraph';

/**
 * The rendered (non-persisted) baseline for a selection.
 */
export interface PolicyTemplatePreview {
  selection?: PolicyTemplateSelection;
  /** Resolved compliance regime, e.g. `eu-gdpr`, `uk-dpa`, `us-baseline`. */
  regime?: string;
  /** The catalog templates composing the graph: [baseline, industry, compliance]. */
  template_ids?: string[];
  /** SHA-256 of the canonical Policy-Graph JSON; the idempotency key. */
  graph_hash?: string;
  /** The rendered, validated deny-by-default Policy-Graph intent. */
  graph?: PolicyTemplatePreviewGraph;
}
