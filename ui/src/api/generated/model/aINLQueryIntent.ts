/**
 * Generated by orval v7.13.2 🍺
 * Do not edit manually.
 * ShieldNet Gateway Control Plane API
 * REST API for ShieldNet Gateway (SNG) — multi-tenant control plane.
All operator endpoints are mounted under `/api/v1`. Auth is
either a bearer JWT (operator console) or `X-SNG-API-Key`
(machine-to-machine).

 * OpenAPI spec version: 0.1.0
 */
import type { AINLQueryIntentKind } from './aINLQueryIntentKind';
import type { AINLQueryIntentWindow } from './aINLQueryIntentWindow';

/**
 * Structured, deterministic-first interpretation of the question. Kind, window and compare_versions are derived deterministically; the LLM may only augment the free-form entity references when present.
 */
export interface AINLQueryIntent {
  kind?: AINLQueryIntentKind;
  user_ref?: string;
  app_ref?: string;
  device_ref?: string;
  /** Enforcement action (access/block) — only set for policy_verdict questions. */
  action?: string;
  /** Normalized relative lookback for analytics questions; absent when the question names no time range. */
  window?: AINLQueryIntentWindow;
  /** Policy graph versions named in a policy_version_compare question, ascending and de-duplicated; empty means the two most recent versions. */
  compare_versions?: number[];
}
