/**
 * Generated by orval v7.13.2 🍺
 * Do not edit manually.
 * ShieldNet Gateway Control Plane API
 * REST API for ShieldNet Gateway (SNG) — multi-tenant control plane.
All operator endpoints are mounted under `/api/v1`. Auth is
either a bearer JWT (operator console) or `X-SNG-API-Key`
(machine-to-machine).

 * OpenAPI spec version: 0.1.0
 */

export interface MobileRefreshRequest {
  /** IdP refresh token; exchanged for a fresh ID token and never persisted. */
  refresh_token: string;
  /** Issuer selecting which provider's token endpoint to call. */
  issuer: string;
  /** Base64 Ed25519 device public key the refreshed session is re-bound to. */
  device_public_key: string;
}
