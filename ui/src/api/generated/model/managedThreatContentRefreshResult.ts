/**
 * Generated by orval v7.13.2 🍺
 * Do not edit manually.
 * ShieldNet Gateway Control Plane API
 * REST API for ShieldNet Gateway (SNG) — multi-tenant control plane.
All operator endpoints are mounted under `/api/v1`. Auth is
either a bearer JWT (operator console) or `X-SNG-API-Key`
(machine-to-machine).

 * OpenAPI spec version: 0.1.0
 */
import type { ManagedThreatContentRefreshSourceStat } from './managedThreatContentRefreshSourceStat';

/**
 * Outcome of a manual managed threat-content refresh.
 */
export interface ManagedThreatContentRefreshResult {
  /** True when the refresh was skipped (kill switch off). */
  skipped: boolean;
  /** True when the assembled content matched the current bundle, so no new version was minted. */
  unchanged: boolean;
  /** Version of the resulting (or current) bundle. */
  serial: number;
  /** Total indicators in the assembled bundle. */
  indicators: number;
  /** True when the bundle was distributed this cycle. */
  published: boolean;
  sources: ManagedThreatContentRefreshSourceStat[];
}
