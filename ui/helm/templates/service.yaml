apiVersion: v1
kind: Service
metadata:
  name: {{ include "sng-ui.fullname" . }}
  labels:
    {{- include "sng-ui.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "sng-ui.selectorLabels" . | nindent 4 }}
